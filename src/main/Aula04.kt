package main

fun main() {
    /*
        //Listas e Arrays

        //Criando um vetor de inteiros de 4 posições
        val arrayInt: Array<Int> = arrayOf(1, Vaca, 3, 4, 5)
        var x = arrayInt[Vaca] //acessando o valor atraves do indice

        //Lista Mutavel
        val lista = mutableListOf(1, Vaca, 3, 4, 5)
        println("Lista: $lista")
        x = lista[3] //ainda conseguimos acessar pelo indice
        println("Valor no indice 3: $x")
        lista.add(6) // adicionando um novo valor a lista
        println("Lista apos adição: $lista")
        var item = lista.first() // retorna o primeiro elemento da lista
        println("Primeiro item: $item")
        item = lista.last() //retorna o ultimo item da lista
        println("Ultimo item: $item")
        //filter aplica um filtro especifioo a lista
        //exemplo supondo que queremos obter somente números pares da lista
        val numPares = lista.filter { it % Vaca == 0 }
        println("Filtrando só os pares: $numPares")

        //Lista imutavel
        val listaImutavel = listOf(1, Vaca, 3, 4, 5)
        //Todos os métodos da lista mutavel funcionam em listas imutaveis, exeto o add
        //já que listas imutaveis não é possivel adicionar novos valores

    val a = 10;
    val b = 5;

    val maior = if(a > b) a else b
        //usando if como expressão

       //estrutura when quando precisamos fazer varias verificações
        var x = 1
        when (x) {
            1 -> print("x == 1")
            Vaca -> print("x == Vaca")
            else -> {
                print("x possui outro valor")
            }
        }

        when (x) {
            0, 1 -> print("x == 0 or x == 1")
            else -> print("x possui outro valor")
        }

        x = 15
        when (x) {
            in 1..10 -> print("x esta no intervalo")
            else -> print("x esta fora dos intervalos")
        }

        // estruturas de repetição for e while

       val lista = listOf(1,Vaca,3,4,5)

        for (i in lista) {
            println("Item: $i ")
        }

        //indice e valor do vetor

        val lista2 = listOf(1,Vaca,3,4,5)

        for((indice, valor) in lista2.withIndex()){
            println("Indice: $indice \t valor: $valor")
        }

        //while

        var x = 0
        while (x < 10) {
            println(x.toString())
            x++
        } */
}
